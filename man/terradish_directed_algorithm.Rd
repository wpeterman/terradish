% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directed_conductance.R
\name{terradish_directed_algorithm}
\alias{terradish_directed_algorithm}
\title{Likelihood of a directed (non-reversible) conductance surface}
\usage{
terradish_directed_algorithm(
  gen,
  g,
  data,
  S,
  par,
  nu = NULL,
  gradient = TRUE,
  phi = NULL,
  nonnegative = TRUE,
  solver = c("matrix", "sparse_lu_cpp")
)
}
\arguments{
\item{gen}{A directed generator model (internal; from
\code{terradish_directed}).}

\item{g}{A \code{terradish} measurement model.}

\item{data}{A \code{terradish_graph}.}

\item{S}{Observed genetic response matrix.}

\item{par}{Numeric vector \code{c(theta, gamma)}.}

\item{nu}{Effective Wishart degrees of freedom (for Wishart models).}

\item{gradient}{Compute the gradient with respect to \code{par}?}

\item{phi}{Optional warm-start nuisance parameters.}

\item{nonnegative}{Force nonnegative measurement-model slope where applicable.}

\item{solver}{Linear solver backend. \code{"matrix"} uses the reference
\pkg{Matrix} implementation; \code{"sparse_lu_cpp"} uses the Eigen
SparseLU C++ backend and reuses each absorber factorization for the
forward and transpose adjoint solves.}
}
\value{
A list with \code{objective}, \code{covariance} (the commute-time
  \code{E}), \code{phi}, and (if \code{gradient}) \code{gradient}.
}
\description{
Forward map and reverse-mode gradient for the directional generator engine.
Builds the directed Markov generator from edge rates, computes the symmetric
commute-time covariance \code{E} among focal sites, evaluates a measurement
model, and returns the profiled objective and its gradient with respect to
\code{(theta, gamma)} via a transpose-solve adjoint.  Users normally call
\code{\link{terradish_directed}} rather than this function.
}
\keyword{internal}
