% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radish_multiscale.R
\name{terradish_multiscale}
\alias{terradish_multiscale}
\alias{radish_multiscale}
\title{Coarse-to-fine optimization for large rasters}
\usage{
terradish_multiscale(
  formula,
  covariates,
  coords,
  factors = c(4L, 2L, 1L),
  directions = 8L,
  aggregate_fun = mean,
  save_surfaces = FALSE,
  ...
)

radish_multiscale(...)
}
\arguments{
\item{formula}{A formula with a genetic-distance matrix on the left-hand
side and raster covariates on the right-hand side.}

\item{covariates}{A \code{SpatRaster} of covariates on the finest grid.}

\item{coords}{Focal coordinates passed to \code{\link{conductance_surface}}.}

\item{factors}{Integer aggregation factors, ordered from coarse to fine.
\code{1} means the original raster resolution.}

\item{directions}{Neighborhood definition passed to
\code{\link{conductance_surface}}.}

\item{aggregate_fun}{Aggregation function for continuous rasters.}

\item{save_surfaces}{If \code{TRUE}, retain the intermediate
\code{terradish_graph} objects.}

\item{...}{Additional arguments passed to \code{\link{terradish}}.}
}
\value{
A fitted \code{terradish} object from the finest raster, with an
additional \code{$multiscale} component containing the per-level fits.
}
\description{
Fits the same resistance-surface model on a sequence of aggregated rasters,
using the estimate from each coarser raster as the starting value for the
next finer raster.
}
\details{
This helper is aimed at large continuous rasters, where a good
coarse-resolution starting point can reduce the amount of full-resolution
optimization work. Factor-valued rasters are not currently supported in the
aggregation path.
}
\examples{
library(terra)

data(melip)
melip.altitude <- terra::unwrap(melip.altitude)
melip.forestcover <- terra::unwrap(melip.forestcover)
melip.coords <- terra::unwrap(melip.coords)

covariates <- c(terra::scale(melip.altitude),
                terra::scale(melip.forestcover))
names(covariates) <- c("altitude", "forestcover")

fit <- terradish_multiscale(
  melip.Fst ~ altitude + forestcover,
  covariates = covariates,
  coords = melip.coords,
  factors = c(2, 1),
  conductance_model = loglinear_conductance,
  measurement_model = generalized_wishart,
  nu = 1000,
  control = NewtonRaphsonControl(maxit = 3, verbose = FALSE)
)
fit$multiscale$factors

}
