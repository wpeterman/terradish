% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical_conductance.R
\name{conductance_field}
\alias{conductance_field}
\title{Extract the fitted conductance field from a hierarchical model}
\usage{
conductance_field(
  fit,
  data,
  type = c("field", "logconductance", "conductance")
)
}
\arguments{
\item{fit}{A \code{"terradish_hierarchical"} object.}

\item{data}{The \code{terradish_graph} used to fit the model.}

\item{type}{\code{"field"} (default) returns the smooth field \eqn{u};
\code{"logconductance"} returns the full fitted \eqn{\log c = X\theta + u};
\code{"conductance"} returns \eqn{\exp(\log c)}.}
}
\value{
A single-layer \code{SpatRaster} (if \code{data$stack} is available)
  or a numeric vector over active cells.
}
\description{
Returns the smooth residual field \eqn{u} (the conductance variation the
covariates did not explain) from a \code{\link{terradish_hierarchical}} fit,
as a \code{terra::SpatRaster} when the graph stored its raster stack, or as a
per-cell numeric vector otherwise.
}
\seealso{
\code{\link{terradish_hierarchical}}
}
