% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radish_cv.R
\name{terradish_cv_replicates}
\alias{terradish_cv_replicates}
\title{Repeated cross validation for terradish models}
\usage{
terradish_cv_replicates(
  pts,
  covariates,
  fmla,
  model = "mlpe",
  nu = NULL,
  prop_train = 0.8,
  n_reps = 5L,
  seeds = NULL,
  fit_full = FALSE,
  keep_fits = FALSE,
  directions = 8,
  conductance_model = loglinear_conductance,
  cores = 1L,
  ...
)
}
\arguments{
\item{pts}{Focal point coordinates as a \code{terra::SpatVector}, matrix, or
data frame with \code{x}/\code{y} columns.}

\item{covariates}{Spatial covariates as a \code{terra::SpatRaster}.}

\item{fmla}{Formula describing the model to assess. The left-hand side must
evaluate to a genetic distance matrix in the calling environment.}

\item{model}{Measurement model, either as a function or one of
\code{"mlpe"}, \code{"wishart"}, or \code{"ls"}.}

\item{nu}{Number of genetic markers, passed to the measurement model.}

\item{prop_train}{Proportion of focal points assigned to the training set.}

\item{n_reps}{Number of repeated train/test splits to evaluate.}

\item{seeds}{Optional integer vector of seeds to use for each replicate. If
supplied, \code{n_reps} is inferred from \code{length(seeds)}.}

\item{fit_full}{Should each replicate also fit a model to all focal points?}

\item{keep_fits}{Should the per-replicate \code{terradish_cv()} outputs be
retained in the returned object?}

\item{directions}{Neighborhood definition passed to
\code{\link{conductance_surface}}.}

\item{conductance_model}{Conductance-model factory used for each replicate.
See \code{\link{terradish_cv}}.}

\item{cores}{Number of worker processes to use in downstream \code{terradish}
fits and grid evaluation.}

\item{...}{Additional arguments passed to \code{\link{terradish}}, such as
\code{control}.}
}
\value{
A list containing a per-replicate summary table, mean and standard
  deviation of held-out log-likelihood, and optionally the individual
  cross-validation fits.
}
\description{
Repeats \code{\link{terradish_cv}} across multiple random splits and
summarizes held-out log-likelihood across replicates.
}
\examples{
library(terra)

data(melip)
melip.altitude <- terra::unwrap(melip.altitude)
melip.forestcover <- terra::unwrap(melip.forestcover)
melip.coords <- terra::unwrap(melip.coords)

keep <- 1:12
melip.Fst_small <- melip.Fst[keep, keep]
covariates <- c(terra::scale(melip.altitude),
                terra::scale(melip.forestcover))
names(covariates) <- c("altitude", "forestcover")

cv_rep <- terradish_cv_replicates(melip.coords[keep], covariates,
                                  melip.Fst_small ~ altitude + forestcover,
                                  model = "ls",
                                  n_reps = 2,
                                  fit_full = FALSE,
                                  control = NewtonRaphsonControl(maxit = 2, verbose = FALSE))
cv_rep$mean_loglik

}
