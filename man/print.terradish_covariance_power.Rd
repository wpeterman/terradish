% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_response_power.R
\name{print.terradish_covariance_power}
\alias{print.terradish_covariance_power}
\title{Print a covariance-response power assessment}
\usage{
\method{print}{terradish_covariance_power}(x, ...)
}
\arguments{
\item{x}{An object of class \code{terradish_covariance_power}, from
\code{\link{covariance_response_power}}.}

\item{...}{Ignored, present for compatibility with the \code{print} generic.}
}
\value{
\code{x}, invisibly.  Called for the printed output.
}
\description{
Compact display for the object returned by
\code{\link{covariance_response_power}}: the settings the sweep ran under,
followed by the scenario-level summary table.
}
\details{
\strong{How to read the output.}  The header records what the sweep did:
how many simulated data sets per design (\code{nsim}), which sample sizes,
and which site-selection strategies.  Judge the numbers below it against
that \code{nsim}: with a small \code{nsim} the Monte Carlo error on a rate
is substantial, so treat a demonstration run as indicative only.

The table beneath is \code{x$summary}, one row per scenario (strategy by
sample size by fitted model).  \code{fit_rate} is the proportion of
simulations that produced a usable fit; a value well below 1 can reflect
weak information, numerical difficulty, boundary fits, or a candidate model
that is poorly matched to the scenario. \code{conductance_power} is the
proportion exceeding the specified conductance-correlation threshold,
\code{mean_conductance_cor} summarizes correlation with the generating
conductance surface, and the model-selection columns report how often each
candidate had the lowest AIC or AICc among usable fits.

Per-coefficient results (power, bias, RMSE, and interval coverage) are not
printed, because there is one row per parameter per scenario.  Read them
from \code{x$parameter_summary}.  Power there is the proportion of
simulations in which the coefficient was both correctly signed and
significant, so it is the quantity to compare against a target such as 0.8.
}
\examples{
\donttest{
data(melip)
covariates <- c(terra::unwrap(melip.altitude), terra::unwrap(melip.forestcover))
names(covariates) <- c("altitude", "forestcover")
# coarsened, and only a handful of simulations, so the example is quick
covariates <- scale_covariates(terra::aggregate(covariates, fact = 3,
                                                na.rm = TRUE))
surface <- conductance_surface(covariates, terra::unwrap(melip.coords),
                               directions = 8, saveStack = TRUE)

power <- covariance_response_power(
  theta        = c(forestcover = 0.5, altitude = -0.4),
  formula      = ~ forestcover + altitude,
  data         = surface,
  sample_sizes = c(8, 12),
  strategies   = "spacefill",
  tau = 1, sigma = 0.2, nu = 100,
  nsim = 3,          # raise to 200+ for a real power estimate
  seed = 1,
  control = NewtonRaphsonControl(maxit = 8)
)

power                     # settings header plus the scenario summary
power$parameter_summary   # per-coefficient power, bias, RMSE, coverage
}

}
\seealso{
\code{\link{covariance_response_power}} for the sweep itself and
  what each returned component contains,
  \code{\link{simulate_covariance_response}} for a single simulated data
  set, and \code{\link{terradish}} for fitting one.  See
  \code{vignette("simulation-design", package = "terradish")} for a worked
  study-design walkthrough.
}
